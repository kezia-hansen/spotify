---
const { variant = "primary", size = "medium" } = Astro.props;
---

<!-- Opsætning af knapvariabler ved hjælp af destrukturering af props ^^^

Knapelement med data-attributter til variant og størrelse -->
<button data-variant={variant} data-size={size}>
  <slot />
</button>

<style>
  /* Styling, der aktiveres, når musen hover over knappen */
  button:hover {
    scale: 1.04;
  }

  /* Variants */
  /* Primær variant med hvid baggrund og sort tekstfarve */
  [data-variant="primary"] {
    background: white;
    color: black;
  }

  /* Sekundær variant med hvid tekstfarve og grå kant */
  [data-variant="secondary"] {
    color: white;
    border: 1px solid #818181;
  }

  /* Tertiær variant med grå tekstfarve */
  [data-variant="tertiary"] {
    color: #818181;
  }

  /* Tertiær variant, når musen svæver over den, med hvid tekstfarve */
  [data-variant="tertiary"]:hover {
    color: white;
  }

  /* Tertiær variant med yderligere styling, hvis den har en søskende med primær variant */
  [data-variant="tertiary"]:has(+ [data-variant="primary"]) {
    padding-inline: 00.5rem 2rem;
  }

  /* Sizes */
  /* Medium størrelse med tilpasset skriftstørrelse og højde */
  [data-size="medium"] {
    font-size: 1rem;
    min-height: 3rem;
    padding-block: 00.5rem;
    padding-inline: 2rem;
  }

  /* Lille størrelse med tilpasset skriftstørrelse og højde */
  [data-size="small"] {
    font-size: 0.875rem;
    min-height: 2rem;
    padding-block: 0.25rem;
    padding-inline: 1rem;
  }
</style>
